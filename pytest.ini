[pytest]
# Disable live cli log capture by default to keep test output concise.
log_cli = false
log_cli_level = INFO
log_level = INFO

# Tests were moved under the package at sofia/tests. Limit discovery there to avoid
# collecting any stale duplicates left at the repository root.
testpaths = sofia/tests
python_files = test_*.py